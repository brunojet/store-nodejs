# Cluster Configuration Guide

## Worker Configuration
# Default: I/O intensive workload (web APIs, database operations)
WORKLOAD_TYPE=io-intensive
WORKER_MULTIPLIER=2
MAX_WORKERS=32

## For CPU intensive workloads (uncomment below)
# WORKLOAD_TYPE=cpu-intensive
# WORKER_MULTIPLIER=1
# MAX_WORKERS=8

## Server Configuration
PORT=3000
NODE_ENV=production

## Performance Tuning Examples:

# High-traffic API server (many concurrent requests)
# WORKER_MULTIPLIER=4
# MAX_WORKERS=64

# Machine Learning/Data Processing (CPU bound)
# WORKLOAD_TYPE=cpu-intensive
# WORKER_MULTIPLIER=1

# Hybrid workload (mix of I/O and CPU)
# WORKER_MULTIPLIER=2
# MAX_WORKERS=16
